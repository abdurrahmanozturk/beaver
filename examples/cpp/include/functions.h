#ifndef FUNCTIONS_H     // prevents duplications (preprocessing)
#define FUNCTIONS_H     // defines header

int add(int,int);
int substract(int,int);
int multiply(int,int);
int divide(int,int);
int readNumber();
void writeAnswer(int);

#endif
